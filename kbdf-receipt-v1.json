{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/mutandrew/schemas/refs/heads/main/kbdf?token=GHSAT0AAAAAADPWS2NACFSKHKGMGCH6JXWQ2I7PX7Q",
  "title": "King's Bench Digital Filing Receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "submission_type",
    "judicial_centre",
    "category",
    "action_number",
    "lawyer_file_number",
    "documents",
    "fees",
    "total",
    "payment"
  ],
  "properties": {
    "submission_type": {
      "type": "string",
      "description": "Type of submission as shown on the receipt",
      "enum": ["Originating", "Subsequent", "Unknown"]
    },
    "judicial_centre": { "type": "string", "minLength": 1 },
    "category": { "type": "string", "minLength": 1 },
    "action_number": {
      "type": "string",
      "description": "e.g., '2501 18402'",
      "pattern": "^[0-9]{4}\\s[0-9]{4,}$"
    },
    "lawyer_file_number": { "type": "string", "minLength": 1 },
    "documents": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/document" }
    },
    "key_dates": {
      "type": "array",
      "description": "Any of next court date, filing deadline, limitation date",
      "items": { "$ref": "#/$defs/key_date" },
      "default": []
    },
    "fees": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tariff_or_filing_fee", "printing_fee"],
      "properties": {
        "tariff_or_filing_fee": { "$ref": "#/$defs/money" },
        "printing_fee": { "$ref": "#/$defs/money" }
      }
    },
    "total": { "$ref": "#/$defs/money" },
    "payment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["order_number", "transaction_id"],
      "properties": {
        "order_number": { "type": "string", "minLength": 1 },
        "transaction_id": { "type": "string", "minLength": 1 }
      }
    },
    "filed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp printed on the receipt PDF, if captured"
    },
    "notes": { "type": "string" }
  },

  "$defs": {
    "money": {
      "type": "number",
      "minimum": 0
    },
    "document": {
      "type": "object",
      "additionalProperties": false,
      "required": ["document_type", "file_name"],
      "properties": {
        "document_type": { "type": "string", "minLength": 1 },
        "file_name": { "type": "string", "minLength": 1 },
        "pages": { "type": "integer", "minimum": 0 }
      }
    },
    "key_date": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "date"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["next_court_date", "filing_deadline", "limitation_date"]
        },
        "date": { "type": "string", "format": "date" }
      }
    }
  }
}
